<!-- DevToolVault_Refatorado/Controls/FileSystemTreeView.xaml -->
<UserControl x:Class="DevToolVault.Controls.FileSystemTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DevToolVault.Converters"
             xmlns:models="clr-namespace:DevToolVault.Core.Models"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <local:IconConverter x:Key="IconConverter" />
    </UserControl.Resources>

    <Grid>
        <TreeView x:Name="treeView" ItemsSource="{Binding Items}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}" DataType="{x:Type models:FileSystemItem}">
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                  IsThreeState="True"
                                  Checked="CheckBox_Checked"
                                  Unchecked="CheckBox_Unchecked" />
                        <!-- Corrigido: Usar ContentPresenter para exibir o PackIcon retornado pelo converter -->
                        <ContentPresenter Content="{Binding Name, Converter={StaticResource IconConverter}}" Width="16" Height="16" />
                        <TextBlock Text="{Binding Name}" Margin="5,0,0,0" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</UserControl>